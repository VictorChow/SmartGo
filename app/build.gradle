apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion "25.0.2"
}

dependencies {
    compile project(':smartgo-annotation')
    annotationProcessor project(':smartgo-compiler')
}
